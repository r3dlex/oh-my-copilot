# Northstar A→B Handoff: file-system-seam

- Spec: `docs/specifications/ACTIVE/file-system-seam.md`
- Sliced goals: see the `plan:` traceability node and `ralplan` output for this slug.
- Issue: .ai/work-intake/file-system-seam.md
- Manifest record: `optional_branches[id=northstar-handoff-file-system-seam]` in `.ai/workflows/repo-workflow.json`.
- Traceability: `plan:*:northstar-file-system-seam` and `handoff:*:northstar-file-system-seam` (schema_version 1.1).

autobahn consumes this handoff to ship each sliced goal one PR at a time.
